import styled from "styled-components";

export const Container = styled.section`

    //span criado para distancia o icone do numero.
    span {
            margin-left: 2px;
            padding: 3px;
        }
`